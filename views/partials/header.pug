header
  h1.title
    a(href=routes.home) #{siteName}
  div.search
    form(action=routes.search, method="get")
      input(type="text", placeholder="검색", name="term", value=searchTerm)
  ul
    li
      a(href=routes.login) Login
    li
      a(href=routes.join) Join
  hr